[project]
name = "mcp-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=1.0.15",
    "kubectl-mcp-tool>=1.2.0",
    "mcp[cli]>=1.12.0",
    "ollama>=0.5.1",
]
